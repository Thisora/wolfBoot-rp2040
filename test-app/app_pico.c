#include <stdlib.h>
#include <stdint.h>
#include <string.h>

#include "wolfboot/wolfboot.h"
// #include "pico/stdlib.h"


int main() {
	// int flashes = 0;
	// const uint LED_PIN = 25;
	// gpio_init(LED_PIN);
	// gpio_set_dir(LED_PIN, GPIO_OUT);
	// while (flashes < 20) {
	// 	gpio_put(LED_PIN, 1);
	// 	sleep_ms(100);
	// 	gpio_put(LED_PIN, 0);
	// 	sleep_ms(100);
	// 	flashes++;
	// }
    return 0;
}
